# ジョブリターン分析ツール システム仕様書

## 1. 全体構造・動作環境
* **システム目的**: AS400（DB2）から退職者データを抽出し、退職理由や属性ごとの傾向をワンタッチで可視化する。
* **データソース**:
  * **メインデータ**: DBテーブル `LIBIMF.IRTP01`（ADODBによるSQL接続）
  * **マスタデータ**: 実行ファイルと同階層に配置された `会社.ini`、`役職.ini`、`所属.ini`（FileSystemObjectで読み込み）
* **終了時の挙動**: ダッシュボードのレイアウト（列幅など）の崩れや意図せぬ上書き保存を防ぐため、専用の終了ボタン（`subCloseSafely`）から「保存せずに終了」する安全設計。

---

## 2. データ抽出・除外ルール（対象者の特定）
旧システムのバグを修正し、純粋な集計対象者のみを正確に拾い上げる厳格なルールを適用。

* **期間絞り込み**: 画面上で指定がある場合のみ、**退職日（`RTTDAT`）** を基準にSQLで抽出期間を絞り込む。
* **除外ルール①（削除データの弾き）**: `RTDLT`（削除フラグ）列に「`*`」等の値が入っている論理削除レコードは完全にスキップする。
* **除外ルール②（不正データの弾き）**: 性別（`RTSEXC`）が「`1`（男性）」「`2`（女性）」以外の空欄や異常値となっているデータはスキップし、ノイズ（不明データ）を排除する。

---

## 3. データ加工・集計ルール（裏側の心臓部）
ピボットテーブルで正確な数値を出すため、「集計用」シートへ書き出す際の最重要ロジック。

* **勤続年数の正確な算出**:
  * 旧仕様のバグ（マクロ実行日を基準にしてしまう問題）を撤廃。
  * **「入社日～退社日」** の純粋な日数差分をもとに正確な在籍年数を計算。
  * **階層区分**: `01.1年未満`, `02.1-3年`, `03.3-5年`, `04.5-10年`, `05.10年以上`
* **アンピボット（複数理由の縦展開）**:
  * 「きっかけ（21項目）」「期待（14項目）」でフラグ（`1`）が立っているものを、それぞれ縦の行データとして展開して書き出す。
* **完全ユニーク化（人数のズレ防止）**:
  * ジョブリターン等で過去に2回以上退職した人を別々の退職イベントとして正確にカウントするため、社員番号ではなく **「行番号（イベント）」をキーにして重複を管理**。
  * 「きっかけ用」「期待用」でそれぞれ独立して、**最初の1回目のみ** `人数フラグ = 1` を立てる。
* **未回答者の救済（ダミー行の生成）**:
  * 理由に1つもチェックを入れていない人も、男女比などの母数から消滅しないよう、「未回答」というダミー行を作成し、確実に「1名」としてカウントする。

---

## 4. 画面描画・ダッシュボードUI（見た目・操作性）
ユーザーの視界から「システム感」や「処理の裏側」を完全に消し去るプロ仕様のデザイン。

* **チラつきの完全排除**: 処理の最初に `Application.ScreenUpdating = False` を呼び出し、裏側ですべてを描画し終えてから一瞬で画面を表示する。
* **デザイン・カラーリング**:
  * **背景色**: ライトブルー（`RGB(141, 180, 226)`）
  * **文字色**: ネイビーブルー（`RGB(0, 32, 96)`）
* **ピボットテーブルの隠蔽**:
  * ユーザーの視界に入らない **「100行目」** にピボットテーブルを生成。
  * 画面右側の「ピボットテーブル フィールドリスト」ポップアップも強制的に非表示化。
* **グラフとスライサーの連動**:
  * メニューのパターン番号に応じて、円グラフ・積み上げ棒グラフ・集合棒グラフへ自動分岐。
  * グラフの各要素（図形）の上に、**実数値（データラベル）を強制表示**。
  * 画面右側に4つのスライサー（会社名、工場名、勤続区分、性別名）を自動生成し、直感的な絞り込み操作を実現。
  * グラフタイトルや画面ヘッダーは、カスタム関数（`fnTrimCode`）により「11.」などの先頭の管理番号をカットして美しく表示。
* **最終フォーカス**: 処理完了後、自動的に「A1セル」にスクロール・選択を戻し、タイトル行がグレー反転して残る現象を回避。

---

## 5. モジュールの役割分担（VBAの構造）
役割を完全に分離し、メンテナンス性を向上。

* **`modMain`（裏方の司令塔）**:
  * データの抽出、マスタの結合、複雑な縦展開（アンピボット）とフラグ立てを担当。
* **`modPivot`（画面の職人）**:
  * 集計用シートのデータを受け取り、ダッシュボードの塗りつぶし、グラフ作成、ピボット生成、カーソル調整など「見た目」のすべてを担当。
* **`modPublic` / `modCommon`（部品庫）**:
  * INIファイル用の辞書オブジェクトや、DB接続文字列、共通関数（`fnTrimCode`など）、共有変数を保管。